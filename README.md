# KNN-Classification
This project implements a K-Nearest Neighbors (KNN) Classification model using Python and Scikit-learn. It includes data preprocessing, feature scaling, model training, hyperparameter tuning with different K values, performance evaluation using accuracy and confusion matrix, and visualization to identify the optimal K value.
